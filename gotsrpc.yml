module:
  name: github.com/foomo/redirects
  path: ./

targets:
  redirect:
    services:
      /services/redirects/redirect: RedirectionService
    package: github.com/foomo/redirects/services/redirect
    tsrpc:
      - RedirectionService
